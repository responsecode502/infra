timestamp_format long
snapshot_dir /.snapshots
snapshot_preserve_min latest
snapshot_preserve 14d 4w 12m

volume /mnt/btrfs-root
    subvolume @
    subvolume @home

snapshot_create after "/usr/bin/grub-mkconfig -o /boot/grub/grub.cfg"
